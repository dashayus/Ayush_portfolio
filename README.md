# Ayushman Dash — Portfolio Website

## 📁 File Structure
```
portfolio/
├── index.html          ← Main HTML file (open this in browser)
├── css/
│   └── style.css       ← All styles
├── js/
│   └── main.js         ← Interactivity (tabs, animations, etc.)
└── assets/
    ├── profile.jpg     ← ⭐ REPLACE THIS WITH YOUR PHOTO
    └── profile-placeholder.svg
```

## 🖼️ Adding Your Profile Photo
1. Get a good portrait photo (recommended: 420×480px or similar)
2. Name it `profile.jpg`
3. Place it in the `assets/` folder
4. The site will automatically use it!

## 🚀 Deploying (Free Options)
- **GitHub Pages**: Push to a GitHub repo → Settings → Pages → Deploy from main branch
- **Netlify**: Drag & drop the folder at netlify.com/drop
- **Vercel**: `npx vercel` in the folder

## ✏️ Customizations
- Update LinkedIn/GitHub URLs in `index.html` (search for `ayushmandash`)
- Change colors in `css/style.css` under `:root { }` variables
- Add more projects by copying a `.proj-card` block

Built with: HTML5 · CSS3 · Vanilla JS · Google Fonts
